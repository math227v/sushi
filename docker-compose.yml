services:
  sushi-counter:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    volumes:
      - sushi-data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  sushi-data:
